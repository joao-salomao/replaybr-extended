services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      WORK_DIR: /app/work
    volumes:
      # Temp files stay outside the container's writable layer.
      - work:/app/work
    restart: unless-stopped

volumes:
  work:
